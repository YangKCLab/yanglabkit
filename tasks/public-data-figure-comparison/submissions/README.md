# Submissions

Place each candidate under an opaque directory name such as `candidate-001`.
Start from `../submission-template/`, keep generator identity only in
`submission.json`, and run the validator before comparison.

Candidate outputs are intentionally not included in the initial scaffold.

