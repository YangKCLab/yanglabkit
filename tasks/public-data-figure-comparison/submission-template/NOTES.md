# Submission notes

- Implementation:
- Dependencies and versions: Use the versions in `uv.lock`; list any non-Python
  runtime dependencies.
- Determinism/reproduction notes: Include the complete
  `uv run --frozen python ...` command for Python submissions.
- Independence statement: Confirm that no competing submission or comparison
  output was accessed, or describe any accidental exposure.
- Known limitations:
