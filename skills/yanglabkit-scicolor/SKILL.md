---
name: yanglabkit-scicolor
user_invocable: true
disable-model-invocation: false
description: >-
  Pick a scientifically sound colour palette for a figure, chart, plot, heatmap,
  map, slide, or website. Given a data/chart scenario, applies the Crameri et al.
  (2020) decision logic to choose a palette class (categorical / sequential /
  diverging / multi-sequential) and returns a concrete palette with actual hex
  codes — discrete or continuous — defaulting to colourblind-safe, perceptually
  uniform choices and refusing rainbow/jet. Trigger when the user asks what
  colours, palette, or colormap to use for a chart/figure/plot/heatmap/map/slide/
  UI, or asks for a colourblind-safe or print/greyscale-safe palette.
---

# yanglabkit-scicolor — Scientific colour-scheme picker

Recommend a colour palette for a visualization by applying the selection logic
from **Crameri, Shephard & Heron (2020), "The misuse of colour in science
communication"** to a curated, self-contained palette dataset drawn from the
lab's **scicolor** collection. Returns real hex codes, defaults to
colourblind-safe and perceptually-uniform palettes, and refuses rainbow/jet.

This skill is pure markdown + data: it reads two bundled JSON files and one
reference guide. **No code execution and no Python dependency at runtime.**

## When to Use

- Choosing colours for a figure, chart, plot, heatmap, map, slide, or UI.
- Needing a colourblind-safe (CVD-safe) or print/greyscale-safe palette.
- Questions like "what colours / colormap should I use for X", "give me a
  colourblind-friendly palette for a bar chart", "which map for a correlation
  heatmap", "is jet/rainbow OK?".

## Workflow

Work through `reference/selection-guide.md`, which holds the class taxonomy,
decision flow, and guardrails. This is the procedure:

1. **Classify + constrain.** Map the data structure to a palette class
   (categorical / sequential / diverging / multi-sequential) and gather
   constraints — category/level count, discrete vs continuous, CVD-safe
   (**default yes**), greyscale/print-safe — following the guide's Steps 1–3.
2. **Query the bundled data.** Discrete/categorical → `data/colors.json`
   (schemes carry a `labels` array and an explicit `colors` hex array).
   Continuous → `data/continuous-colormaps.json` (each entry has `cm_class`,
   `perceptually_uniform`, `color_blind_friendly`, and 32 hex `stops`). Filter by
   class + `color_blind_friendly` (when CVD-safe is required) and sufficient
   colour count; the guide's Step 4 names concrete palettes per class.
3. **Return the recommendation.** 1–2 palettes with **actual hex codes** (the
   discrete hex array, or the continuous hex stops) + palette name + source
   collection. For continuous maps, also give the canonical name so the user can
   pull the full-resolution object via `scicolor.get_cmap(name)` / matplotlib.
4. **Enforce the guardrails** in the guide's Step 5 before finalising.

## Rules

- **Default to colourblind-safe.** Only relax if the user explicitly opts out.
- **Always return actual hex codes** — never names alone — with the palette name
  and its source collection (e.g. "Okabe–Ito", "Scientific colour maps / vik").
- **Never rainbow, jet, HSV, or Turbo**, and never red–green at equal luminosity.
- **Enforce the full guardrail checklist** (perceptual uniformity, perceptual
  order / greyscale-safe, colour bar for continuous maps) from the guide's Step 5.

## Data & provenance

See `data/SOURCE.md` for where the two data files come from and how to re-sync
them. The continuous file is generated by the repo-level
`scripts/extract_continuous_colormaps.py` (a build-time-only script kept outside
the skill folder — not read at runtime).
